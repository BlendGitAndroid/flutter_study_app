import 'package:flutter/material.dart';

/// 欢迎人展示组件
class WelcomeSub extends StatelessWidget {
  /// 有状态类返回组件信息
  @override
  Widget build(BuildContext context) {
    print('welcome Sub build');

    return Text('欢迎 WelcomeSub');
  }
}
